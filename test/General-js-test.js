var assert = require("assert");
var should = require('should');

describe('General-js-test', function() {
    before(function() {
    })

    after(function() {
    })

    describe('JS Module isolation', function() {
        it('Should be isolated', function() {
        })
    })

    //
    // See
    //  https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/typeof
    //  https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/instanceof
    //  https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Details_of_the_Object_Model
    //
    describe('JS Class', function() {
        it('Should be isolated', function() {
        })

        it('Should be constructable', function() {
        })

        it('Should be an Object', function() {
            // typeof
        })

        it('Should be kind of a class', function() {
            // instanceof
        })

        it('Should access to a public method', function() {
        })

        it('Should not access to a private method', function() {
        })

        it('Should access to a public property', function() {
        })

        it('Should not access to a private property', function() {
        })

        it('Should access to a instance property', function() {
        })

        it('Should access to a static property', function() {
        })
    })
})
